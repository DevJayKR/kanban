version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - npm run build
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - '**/*'